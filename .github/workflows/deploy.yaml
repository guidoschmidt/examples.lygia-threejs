name: Build
on:
  push:
    branches:
      - main
  pull_request: {}

jobs:
  deploy:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: npm build
